pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                    sh 'mvn package'
            }
        }
         stage('Run') {
            steps {
                 sh 'java -jar -Dserver.port=8083 target/xmltohtml-0.0.1-SNAPSHOT.jar'
            }
}
}
}
